using UnityEngine;
using System.Collections;
public class AtkParticles : MonoBehaviour
{

    // ParticleSystem pSys;

    // // Start is called once before the first execution of Update after the MonoBehaviour is created
    // // void Start()
    // // {
    // //     pSys = this.GetComponent<ParticleSystem>();
    // //     // Emitter.startLifetime = Vector3.Distance(Emitter.transform.position,target.position)/Emitter.startSpeed;
    // // }

    // // public void PlayDirectionalParticleWave (Vector3 velocity) {
    // //     var vel = pSys.velocityOvertime;
    // //     if (!vel.enabled) {vel.enabled = true;}

    // //     vel.space = ParticleSimulationSpace.World;

    // //     //TODO check in practice with animation on shark
    // //     vel.orbitalXMultiplier = velocity.x;
    // //     vel.orbitalYMultiplier = velocity.y;
    // //     vel.orbitalZMultiplier = velocity.z;
    // // }
}
